# Haag_ng_2024

This repo has the scripts necessary to run LDA using the implementation from sklearn. The defaults set in LDAmodel.py work well most of the times.

It is important that the matrix has the raw data stored in the "raw" slot.